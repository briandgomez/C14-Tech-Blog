<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Main Page</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
</head>

<body>
    <div class="wrapper">
        <header>
            <nav>
                {{#if loggedIn}}
                <a href="../dashboard.handlebars"></a>
                {{else}}
                <a href="../homepage.handlebars"></a>
                {{/if}}
            </nav>
        </header>
        <main>
            {{{ body }}}
        </main>
    </div>

    {{#if loggedIn}}
    <script src="../../public/javascript/logout.js"></script>
    {{/if}}
</body>

</html>